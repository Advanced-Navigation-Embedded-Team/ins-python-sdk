name: publish package
run-name: ${{ github.actor }} is updating the PyPi repo 🚀
on: [push] 
jobs:
  Publish-Using-Poetry:
    runs-on: ubuntu-latest
    steps:
    - run: sudo apt install pipx
    - run: pipx install poetry
    - run: poetry config pypi-token.pypi ${{ secrets.PYPI_SCOPED_KEY }}
    - run: echo 'changing directory'
    - run: cd advanced_navigation
    - run: poetry build
    - run: poetry publish
